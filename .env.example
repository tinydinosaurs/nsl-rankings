# Server
PORT=3001
NODE_ENV=development

# Auth — use a strong secret in production; never commit .env
JWT_SECRET=change-me-in-production

# CORS — frontend origin for API requests
CLIENT_URL=http://localhost:5173
